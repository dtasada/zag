import lib;

struct MyStruct {
    x, y: i32,
    z: f32
}

fn my_func(x, y: i32, z: f32) void {

}

fn main() i32 {
    let a = 1;
    let b = 2;
    let c = 3;
    let d = 4;
    if (a < b != c < d) {
        lib.printf("hello there\n");
    }
}
