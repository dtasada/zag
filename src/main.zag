struct MyStruct {
	x: i32,
	y: i32,

    fn setX(self: &mut MyStruct, new_x: i32) void {
        self.x = new_x;
    }

	fn add(lhs: MyStruct, rhs: MyStruct) MyStruct {
		return MyStruct {
			x: lhs.x + rhs.x,
			y: lhs.y + rhs.y,
		};
	}
}

fn main() i32 {
	let mut array = [_]f32{1, 2, 3};

    let c = 420;
}
