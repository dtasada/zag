import std;
import c;

struct MyStruct {
    a: i32,
}

const T = MyStruct;

fn main() i32 {
    let mut ar = std.ArrayList<i32>.init();
    ar.append(42);
    c.printf("item 0: %d\n", ar.items[0]);
    ar.append(69);
    c.printf("item 1: %d\n", ar.items[1]);

    ar.deinit();

    return 0;
}

