import std;
import c;

struct TestStruct {
    struct SubType {
        inner: TestStruct,
    }

    x: SubType,
}

fn main() i32 {
    let mut arraylist = std.ArrayList<i32>.init();
    arraylist.deinit();

    return 0;
}

