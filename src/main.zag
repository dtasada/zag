import lib;

fn generic_func<T>() void {

}

fn main() i32 {
    let v2a = lib.Vector2.init(1, 2);
    let v2b = lib.Vector2.init(1, 2);

    let v2c = v2a.add(v2b);

    let myenum = lib.MyEnum.a;

    generic_func<i32>();

    lib.printf("Hello there!\n");
    lib.do_something();
}
